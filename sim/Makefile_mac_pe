SIM ?= icarus
TOPLEVEL_LANG ?= verilog

VERILOG_SOURCES += $(PWD)/../rtl/mac_pe.v

TOPLEVEL = mac_pe
MODULE = test_mac_pe
SIM_BUILD = sim_build_mac_pe

include $(shell cocotb-config --makefiles)/Makefile.sim
